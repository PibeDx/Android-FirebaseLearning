<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebaselearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidFirebaseLearning">
        <activity android:name=".presentation.feature.premenu.view.PreMenuActivity"></activity>
        <activity
            android:name=".presentation.feature.menu.view.MenuActivity"
            android:label="@string/title_activity_menu" />
        <activity android:name=".presentation.feature.authencation.login.view.LogInActivity" />
        <activity android:name=".presentation.feature.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>